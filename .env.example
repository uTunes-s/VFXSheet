# Netlify site configuration: Site configuration > Environment variables
# Never commit the production values into Git.
FLOWPT_SYNC_URL=https://your-flowpt-api.example/api/vfx/sync
FLOWPT_API_TOKEN=replace-with-a-server-side-token
