// Active camera settings form rendering.
import { state } from './state.js';
import { escapeHtml } from './utils.js';
import { getCameraFieldLabel, getCameraReelName } from './camera-model.js';
import { cachedCamPresets, cachedLensPresets, cachedMovementPresets } from './preset-store.js';
import { renderPresetOptions } from './preset-options.js';

export function renderCameraTabContent() {
  const container = document.getElementById('cameraTabContents');
  const { cameraListState, activeCamIndex } = state;
  const camera = cameraListState[activeCamIndex];
  if (!camera) return;
  const label = getCameraFieldLabel(camera);
  const customClass = value => value === '__custom__' ? 'w-1/2' : 'w-full';
  const customInput = (id, value, placeholder, visible) => `<input type="text" id="${id}" value="${escapeHtml(value || '')}" placeholder="${placeholder}" class="form-input ${visible ? 'w-1/2' : 'hidden'}">`;
  const field = (key, fieldLabel, placeholder, extra = '') => `<div><label class="form-label" data-initial-setting="camera_${activeCamIndex}_${key}">${fieldLabel}</label><input type="text" id="tab_${key}" value="${escapeHtml(camera[key] || '')}" placeholder="${placeholder}" class="form-input ${extra}"></div>`;
  const pickerField = (key, fieldLabel, placeholder, values, inputMode = 'text') => `<div class="camera-setting-field"><label class="form-label" data-initial-setting="camera_${activeCamIndex}_${key}">${fieldLabel}</label><input type="text" id="tab_${key}" value="${escapeHtml(camera[key] || '')}" placeholder="${placeholder}" inputmode="${inputMode}" autocomplete="off" data-action="toggle-camera-setting-popover" data-field-key="${key}" aria-haspopup="dialog" aria-expanded="false" aria-controls="${key}Popover" class="form-input font-mono"><div id="${key}Popover" class="camera-setting-popover hidden" role="dialog" aria-label="Common ${fieldLabel} values"><div class="camera-setting-options">${values.map(value => `<button type="button" data-action="set-camera-setting" data-field-key="${key}" data-setting-value="${value}" class="camera-setting-option ${camera[key] === value ? 'is-selected' : ''}">${value}</button>`).join('')}</div></div></div>`;
  const flag = (key, flagLabel) => `<div class="bg-slate-900 p-2 rounded-lg border border-slate-800 flex items-center justify-between"><span class="text-xs text-slate-300">${flagLabel}</span><label class="relative inline-flex items-center cursor-pointer" title="${flagLabel}"><input type="checkbox" id="tab_${key}" ${camera[key] === 'YES' ? 'checked' : ''} class="sr-only peer" aria-label="${flagLabel} captured"><div class="w-11 h-6 bg-slate-800 rounded-full peer peer-focus:outline-none peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-slate-300 after:bg-slate-300 after:transition-all peer-checked:bg-amber-500"></div></label></div>`;
  const movementItems = (cachedMovementPresets.some(item => item.name === camera.cramerawork_preset) ? [] : (camera.cramerawork_preset && camera.cramerawork_preset !== '__custom__' ? [{ name: camera.cramerawork_preset }] : [])).concat(cachedMovementPresets);
  container.innerHTML = `
    <div class="text-xs font-bold text-amber-400/90 pb-1 border-b border-slate-800/60">${label} Settings</div>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
      <div><label class="form-label" data-initial-setting="camera_${activeCamIndex}_camera_preset">${label} - Camera Model</label><div class="flex gap-2"><select id="tab_camera_preset" data-action="tab-preset-change" data-preset-type="camera" class="form-select ${customClass(camera.camera_preset)}"><option value="">(Select)</option>${renderPresetOptions(cachedCamPresets, camera.camera_preset)}<option value="__custom__" ${camera.camera_preset === '__custom__' ? 'selected' : ''}>Custom / Other</option></select>${customInput('tab_camera_custom', camera.camera_custom, 'Camera Model', camera.camera_preset === '__custom__')}</div></div>
      <div><label class="form-label" data-initial-setting="camera_${activeCamIndex}_lens_preset">${label} - Lens Model</label><div class="flex gap-2"><select id="tab_lens_preset" data-action="tab-lens-change" class="form-select ${customClass(camera.lens_preset)}"><option value="">(Select)</option>${renderPresetOptions(cachedLensPresets, camera.lens_preset, true)}<option value="__custom__" data-focal="" ${camera.lens_preset === '__custom__' ? 'selected' : ''}>Custom / Zoom</option></select>${customInput('tab_lens_custom', camera.lens_custom, 'Lens Model', camera.lens_preset === '__custom__')}</div></div>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-5 gap-3">${field('focal_length', 'Focal Length', '35mm', 'font-mono')}${pickerField('t_stop', 'Aperture (T-Stop)', 'T2.8', ['T1.4', 'T2', 'T2.8', 'T4', 'T5.6', 'T8'])}${pickerField('shutter_speed', 'Shutter Speed / Angle', '1/50s', ['1/24s', '1/48s', '1/50s', '1/60s', '1/96s', '1/120s', '144°', '172.8°', '180°', '216°', '360°'])}${pickerField('frame_rate', 'Frame Rate', '24fps', ['23.976', '24', '25', '29.97', '30', '50', '59.94', '60'], 'decimal')}</div>
    <div class="grid grid-cols-2 md:grid-cols-5 gap-3">${pickerField('iso_ei', 'ISO / EI', 'ISO 800 / EI 400', ['ISO 400', 'ISO 800', 'ISO 1250', 'ISO 1600', 'EI 400', 'EI 800', 'EI 1280'], 'numeric')}${pickerField('white_balance', 'White Balance', '5600K', ['3200K', '4300K', '5600K', '6000K', '6500K', '7500K'], 'numeric')}${pickerField('nd_filter', 'ND Filter', 'ND 0.6 / 2-stop', ['Clear', 'ND 0.3', 'ND 0.6', 'ND 0.9', 'ND 1.2', 'ND 1.5', 'ND 1.8'])}${field('clip_name', 'Clip Name', `${getCameraReelName(camera)}001C001`, 'font-mono')}${field('lut_info', 'LUT Info', 'SL3SG3cine to Rec709')}</div>
    <div class="grid grid-cols-2 md:grid-cols-5 gap-3"><div><label class="form-label" data-initial-setting="camera_${activeCamIndex}_cramerawork_preset">${label} Movement</label><div class="flex gap-2"><select id="tab_cramerawork_preset" data-action="tab-preset-change" data-preset-type="cramerawork" class="form-select ${customClass(camera.cramerawork_preset)}"><option value="">(Select)</option>${movementItems.map(item => `<option value="${escapeHtml(item.name)}" ${camera.cramerawork_preset === item.name ? 'selected' : ''}>${escapeHtml(item.name)}</option>`).join('')}<option value="__custom__" ${camera.cramerawork_preset === '__custom__' ? 'selected' : ''}>Custom / Other</option></select>${customInput('tab_cramerawork_custom', camera.cramerawork_custom, 'Movement', camera.cramerawork_preset === '__custom__')}</div></div>${field('height_value', `${label} Lens Height`, '150cm', 'font-mono')}${field('distance_value', `${label} Target Distance`, '3.5m', 'font-mono')}${pickerField('tilt_value', `${label} Tilt Angle`, '+10° / -5°', ['-30°', '-10°', '-5°', '-3°', '-2°', '-1°', '0°', '+1°', '+2°', '+3°', '+5°', '+10°', '+30°'])}</div>
    <div class="pt-2 border-t border-slate-800/60"><label class="form-label font-semibold text-amber-400/80 uppercase" data-initial-setting="camera_${activeCamIndex}_reference_flags">${label} Reference Checklist</label><div class="grid grid-cols-3 gap-3">${flag('flag_chart', 'Color Chart & Sphere')}${flag('flag_cleanplate', 'Clean Plate')}${flag('flag_reference', 'VFX Reference')}</div></div>`;
}

