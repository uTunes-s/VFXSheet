// Lens catalog activation boundary.
// The immutable source array remains preserved in the temporary legacy data
// binding until the final data-file cleanup; consumers use this external API.
const defaultLenses = defaultLensesLegacy;
